alias awesome="echo you are awesome"

# -------------------------------------------------------------------
# Fasd
# -------------------------------------------------------------------
alias z="fasd_cd -d -i"

# -------------------------------------------------------------------
# Pure awesome
# -------------------------------------------------------------------
alias tf="terraform"
alias vim="nvim"
alias tmux="tmux -u"
alias please="sudo"
alias bashrc=". ~/.bashrc; . ~/.zshrc; echo 'Bashrc reloaded...'"
alias fuck='eval $(thefuck $(fc -ln -1))'
alias ra='ranger'
alias weather='curl -4 http://wttr.in/Tallinn'
alias av='source ./venv/bin/activate'
alias t=". ~/.bash/todoist-wrapper.sh"
alias fzf="fzf --preview 'head -$LINES {}' --color dark"

# -------------------------------------------------------------------
# osx only
# -------------------------------------------------------------------
alias chromium-browser="open -a Google\ Chrome\ Canary.app"

# -------------------------------------------------------------------
# System aliases
# -------------------------------------------------------------------
alias h="history"
alias ports="lsof -i -n -P"       # Show ports information
alias grep='grep --color=auto'    # Always colored grep

# -------------------------------------------------------------------
# OTHER aliases
# -------------------------------------------------------------------
# Display the battery information
alias battery="~/.bash/battery/battery"
alias cl="clear"            # Clear
alias mkdir="mkdir -p"      # Make all dirs
alias j="jobs -p"           # All jobs + progresses
alias bc='bc -l'            # Calculator
alias pa="ps aux"           # Ps aux
alias pag="ps aux | grep"   # Ps aux grep
